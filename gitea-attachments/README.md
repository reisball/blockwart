# Archived Gitea Issue Attachments

This branch preserves the seven attachments exported from Gitea during the
Blockwart migration to GitHub.

The exact source identity, byte size and SHA-256 digest for every file are
recorded in `export-manifest.json`. Issue bodies and comments use immutable
links to the commit containing these files.

The application source remains unchanged on `main`.
