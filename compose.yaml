services:
  blockwart:
    build: .
    environment:
      BLOCKWART_ENV: production
      BLOCKWART_DATABASE_URL: sqlite:////data/blockwart.sqlite3
      BLOCKWART_SECRET_REFERENCE: vaultwarden:Blockwart
    ports:
      - "8000:8000"
    volumes:
      - /opt/blockwart-data:/data
    restart: unless-stopped
